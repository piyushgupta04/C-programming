// Variables
/*
Variables is the name of the memory location which stores some data 
*/
// Rules
/*
a) Variables are case sensitive
b) 1st character is alphabet or '_'
c) No comma or blank space
d) No other symboles other then '_' 
*/

// # include <stdio.h>
// int main(){
//     int number = 5315;
//     /*
//     int my_age = 18;
//     int my_number = 7398214285;
//     int my_alt_number = 9555012112;
//     */
//     // char hash = '#';
//     // float pi = 3.14;
//     printf("%d",number);
//     return 0;
// }

// Constants
/*
Values that don't change (fixed)
1> Integer Constants
2> Real Constants
3> Character Constants
*/

// Assignment Operator
// 1) =
// 2) +=
// 3) -=
// 4) *=
// 5) /=
// 6) %=

/*
# include <stdio.h>
int main () {
    int a = 20, b = 10;
    a += b; // Same as a = a + b.
    a -= b; // Same as a = a - b.
    a *= b; // Same as a = a * b.
    a /= b; // Same as a = a / b.
    a %= b; // Same as a = a % b.
    printf("%d", a);
    return 0;
}
*/
//  Relational Operator

/*
1- == Equal to 
2- >,>= Greater then, Greater then or equal to
3- <,<= Less then, Less then or equal to
4- != Not equal to
*/

// Equal to [ == ]
/*
# include <stdio.h>
int main (){
    int num1, num2;
    printf("Enter first number: ");
    scanf("%d", &num1);
    printf("Enter second number: ");
    scanf("%d", &num2);
    printf("Output: %d\n||1 = True ||\n||0 = False||", num1 == num2);
    return 0;
}
*/

// Greater then or Greater then equal to [ >, >= ]
/*
# include <stdio.h>
int main (){
    int num_a, num_b;
    printf("Enter first number: ");
    scanf("%d", &num_a);
    printf("Enter second number: ");
    scanf("%d", &num_b);

    printf("Output: %d\n[ 1 = True ]\n[ 0 = False ]", num_a>num_b);
    printf("\n||Your 1st number is %d||,\n||And 2nd number is %d||", num_a, num_b);
    return 0;
}
*/
// Smaller then or smaller then equal to.

# include <stdio.h>
int main (){
    
}